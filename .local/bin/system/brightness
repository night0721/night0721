#!/bin/sh

send_notification() {
	brightness=$(printf "%.0f\n" "$(brightnessctl i | grep 'Current brightness' | awk -F'[(%]' '{print $2}')")
	luft "Brightness: $brightness%"
}

case $1 in
up)
	brightnessctl s +5%
	send_notification
	;;
down)
	brightnessctl s 5%-
	send_notification
	;;
esac
