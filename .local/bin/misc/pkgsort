#!/bin/bash

pacman -Qie | awk -F': ' '/^Name/ {name=$2} /^Install Date/ { "date -d \""$2"\" \"+%s\"" | getline timestamp; print timestamp, name }' | sort -n | awk '{print strftime("%Y-%m-%d %H:%M:%S", $1), $2}'
